const getAllUsers = () => {
    return "Fetching all users"
}

const addNew = () => {
    return "Added a new user"
}

const getUserwithId = (userId) => {
    
    return `Fetching user with ID: ${userId}`
}


module.exports = {getAllUsers, addNew, getUserwithId}